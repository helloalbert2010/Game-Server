# 数据库连接字符串 (从 Neon 获取)
DATABASE_URL=postgresql://username:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require

# JWT 密钥 (生产环境必须设置，使用随机字符串)
JWT_SECRET=your-random-jwt-secret-key-here

# 环境设置
NODE_ENV=production

# 端口 (可选，Vercel 会自动设置)
PORT=3005
